name: Update downstream repositories

on:
  push:
    branches:
      - main

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      actions: write

    steps:
      - name: Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.11.0
        with:
          access_token: ${{ github.token }}

      - name: Checkout acquire-driver-common
        uses: actions/checkout@v3
        with:
          repository: 'acquire-driver-common'
          submodules: true

      - name: Update the acquire-core-libs submodule
        run: |
          pushd src/acquire-core-libs
          git checkout ${{ github.sha }}
          popd
          git add src/acquire-core-libs
          git commit -m "(automated) update acquire-core-libs"
          git push
        working-directory: ${{github.workspace}}/acquire-driver-common
